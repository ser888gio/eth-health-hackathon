[project]
name = "ingestion"
version = "0.1.0"
description = "RAG ingestion pipeline for txt files into pgvector"
requires-python = ">=3.12"
dependencies = [
    "sentence-transformers>=3.0",
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
]
